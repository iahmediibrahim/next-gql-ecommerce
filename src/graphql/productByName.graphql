query productByName($name: String!) {
	product(name: $name) {
		id
		name
		category
		price
		currency
		image {
			src
			alt
		}
		bestseller
		featured
		details {
			weight
			thickness
			description
			recommendations {
				src
				alt
			}
		}
	}
}
